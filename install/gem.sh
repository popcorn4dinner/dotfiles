gem install rails
gem install spring
gem install capistrano capistrano-npm
gem install sinatra
gem install 
gems=(
    bundler
    capistrano
    capistrano-npm
    minitest
    rails
    rspec
    rubocop
    sinatra
    spring
)

gem install "${gems[@]}"
